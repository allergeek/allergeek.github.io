// uses data from theme/_data.json

p This is a theme page.
  | Here's how theme titles are inserted (also, see the empty values):

div
  for key, theme in public.post._data
    a( href="/post/#{ theme }" )
      span #{ key.theme }
    | .

div
  for key, theme in public.theme._data
    a( href="/post/#{ theme }" )
      span= key.theme
    br

p Examples of referencing:
p= titles[0]

for title in titles
  p= title

br

p Post metadata (properties):

each ref, index in refs
  p
    a( href="/theme/running-theme/#{ ref }" )
      span uri
  span date - #{ dates [index] }
  br
  br
  span tags - #{ tags [index] + [' hey' ] }

  br
  br

  span url - #{ ref }

  br
  br

  span one by one:
  each ttt, index2 in tags
    span #{ tags [index] [index2] + [' ']}

  br
  br


p ALL tags, post by post, tag by tag
  br
  | need to know length of the array (number of tags),
  br
  | to write a dynamic function

each tag, index in tags
  span post N #{ [index + 1] }
  br
  span #{ tags [index] [0] }
  br
  span #{ tags [index] [1] }
  br
  span #{ tags [index] [2] }
  br
  br


// ! gets the key VALUES out !

for any, key in public.post._data
  p= [key]
