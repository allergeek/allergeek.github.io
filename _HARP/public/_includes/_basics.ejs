<%# parent only returns one folder name %>
<%
  parent = current.path[current.path.length - 2];

  themesData = public.theme._data;
  themesRU = [];
  themesEN = [];
  themesRUpath = [];

  for (var theme in themesData) {
    var pref = themesData[theme];
    if (pref.lang === "ru") {
      themesRU.push(pref.ru);
      themesRUpath.push(theme);
    }
  };

  for (var theme in themesData) {
    var pref = themesData[theme];
    if (pref.lang !== "ru" && pref.index !== true ) {
      themesEN.push(theme);
    }
  };
%>

<%
  postsEN = [];
  postsRU = [];
  postsENpath = [];
  postsRUpath = [];
  postsENthemes = [];
  postsRUthemes = [];

  slug = [];
  loot = 0;


  for (var i = 0, l = themesEN.length; i < l; i++) {
    var source;
    source = "public.post." + themesEN[i] + "._data";
    slug.push(source);
  }

    loot = public.post.alcohol._data;
    for (var post in loot) {
      if (loot[post].lang !== "ru") {
        postsEN.push(loot[post].title);
        postsENpath.push(post);
        postsENthemes.push(loot[post].theme.toLowerCase());
      }
    };

    loot = public.post.neuroscience._data;
    for (var post in loot) {
      if (loot[post].lang !== "ru") {
        postsEN.push(loot[post].title);
        postsENpath.push(post);
        postsENthemes.push(loot[post].theme.toLowerCase());
      }
    };

    loot = public.post.nutrition._data;
    for (var post in loot) {
      if (loot[post].lang !== "ru") {
        postsEN.push(loot[post].title);
        postsENpath.push(post);
        postsENthemes.push(loot[post].theme.toLowerCase());
      }
    };

    loot = public.post.quitting._data;
    for (var post in loot) {
      if (loot[post].lang !== "ru") {
        postsEN.push(loot[post].title);
        postsENpath.push(post);
        postsENthemes.push(loot[post].theme.toLowerCase());
      }
    };

    loot = public.post.riding._data;
    for (var post in loot) {
      if (loot[post].lang !== "ru") {
        postsEN.push(loot[post].title);
        postsENpath.push(post);
        postsENthemes.push(loot[post].theme.toLowerCase());
      }
    };

    loot = public.post.running._data;
    for (var post in loot) {
      if (loot[post].lang !== "ru") {
        postsEN.push(loot[post].title);
        postsENpath.push(post);
        postsENthemes.push(loot[post].theme.toLowerCase());
      }
    };

    loot = public.post.sleep._data;
    for (var post in loot) {
      if (loot[post].lang !== "ru") {
        postsEN.push(loot[post].title);
        postsENpath.push(post);
        postsENthemes.push(loot[post].theme.toLowerCase());
      }
    };



  loot = public.post.neuroscience._data;
  for (var post in loot) {
    if (loot[post].lang === "ru") {
      postsRU.push(loot[post].title);
      postsRUpath.push(post);
      postsRUthemes.push(loot[post].path.slice(0,-3));
    }
  };

  loot = public.post.running._data;
  for (var post in loot) {
    if (loot[post].lang === "ru") {
      postsRU.push(loot[post].title);
      postsRUpath.push(post);
      postsRUthemes.push(loot[post].path.slice(0,-3));
    }
  };
%>

<%- include _helpers %>
