// This is a post template
// Jade files store content. Every text post should have its own .JADE file

script.
  if (10 == 10) {
    //alert("working, too");
  }


-
  var localVar =
  "localVar var: here's that unique post title!"

//-
  //h3= say_hello(localVar)
  h2= localVar
  h2= title
  h3 some string content in place of yield
  span string theme
  p string <b><i>post</i></b> intro
