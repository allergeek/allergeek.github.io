<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="../../css/reset.css">
    <%- include ../_includes/_head %>
    <%- include ../_includes/_post-css %>
  </head>
  <body>
    <% include ../_includes/_menu %>
    <div class="body-container start-margin post-margin">
      <div class="fixed-bar">
        <a href="#top"><div class="bar-wrapper"><span class="title-bar"><%= title %></span><span class="top-icon">&#10141;</span></div></a>
      </div>
      <div class="centered">
        <div class="remember before-post">
          <a href="<%- mailtoStr() %>" class="action-btn check" data-descr="Memorize the stuff by getting a short note sent to your inbox. It's something I use myself, opt out any time."><span>&check;</span>Help me remember this</a>
          <div class="print-ctrl">
            <% include ../_includes/_cards %>
            <% include ../_includes/_post-print %>
          </div>
        </div>
        <% include ../_includes/_post-header %>
        <%# If the post is marked as a draft then show a draft card %>

        <% if (typeof draft !== "undefined" && draft) { %>
          <% include ../_includes/_post-draft %>
        <% } %>

        <%# If the post notes have single origin then show source card %>

        <% if (origin == "single") { %>
          <% include ../_includes/_post-card %>
        <% } %>

        <%# if(environment == "production") { %>
        <div class="page-body">
          <%- yield %>
        </div>
        <%# } %>

        <div class="remember">
          <a href="<%- mailtoStr() %>" class="action-btn check" data-descr="Memorize the stuff by getting a short note sent to your inbox. It's something I use myself, opt out any time."><span>&check;</span>Help me remember this</a>
          <div class="print-ctrl">
            <% include ../_includes/_cards %>
            <% include ../_includes/_post-print %>
          </div>
        </div>

      <% include ../_includes/_post-next %>
      <% include ../_includes/_post-outro %>
      <% include ../_includes/_post-chimp %>


      <% if (typeof references !== "undefined" && references) { %>
        <script src="../../ref.js"></script>
        <script>
          // console.log('yep')
          // show a certain html file
          // get all h2
          // and fill one by one
          // in a certain element
        </script>
        <%# console.log('yes!!') %>
        <% for (slug in public) { %>
          <%  %>
        <% } %>
        <% for (i = 0; i < 0 ; i++) { %>
          <%  %>
        <% } %>
      <% } %>
      </div>
    </div>
  </body>
</html>
